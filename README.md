
  # Stock Recommendation Feature

  This is a code bundle for Stock Recommendation Feature. The original project is available at https://www.figma.com/design/jwog62tQ14guAKitKyzRV2/Stock-Recommendation-Feature.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  